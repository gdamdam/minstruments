{
  "name": "minstruments",
  "version": "0.1.2",
  "private": true,
  "description": "m//instruments — independent music instruments for the browser.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
