{
  "name": "m-suite",
  "version": "0.1.4",
  "private": true,
  "description": "m-suite — a collection of local-first browser instruments and music tools.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
